/// AppValues: Centralized numeric values, durations, paddings, etc.
class AppValues {
  static const defaultPadding = 24.0;
  static const defaultRadius = 12.0;
  static const buttonHeight = 48.0;
  static const iconSize = 36.0;
  static const snackBarDuration = Duration(milliseconds: 500);
  // ... add more as needed
}
